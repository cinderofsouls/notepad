<Window x:Class="Notepad.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Notepad"
        mc:Ignorable="d"
        Title="{Binding WindowTitle}" Height="450" Width="800">
    <Window.DataContext>
        <local:MainWindowViewModel/>
    </Window.DataContext>
    <Window.Resources>
        <local:BooleanToWordWrapConverter x:Key="BooleanToWordWrapConverter"/>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>
        <Menu Grid.Row="0">
            <MenuItem Header="_File" Height="20">
                <MenuItem Header="_New" Command="{Binding NewCommand}" Height="20"/>
                <MenuItem Header="_Open" Command="{Binding OpenCommand}" Height="20"/>
                <MenuItem Header="_Save" Command="{Binding SaveCommand}" Height="20"/>
                <MenuItem Header="Save _As" Command="{Binding SaveAsCommand}" Height="20"/>
                <Separator/>
                <MenuItem Header="_Exit" Command="{Binding ExitCommand}" Height="20"/>
            </MenuItem>
            <MenuItem Header="_Edit">
                <MenuItem Command="Copy" Height="20"/>
                <MenuItem Command="Cut" Height="20"/>
                <MenuItem Command="Paste" Height="20"/>
            </MenuItem>
            <MenuItem Header="_Format">
                <MenuItem Header="_Wrap" IsCheckable="True" IsChecked="{Binding WordWrap}" Height="20"/>
            </MenuItem>
        </Menu>
        <TextBox Text="{Binding MainText, UpdateSourceTrigger=PropertyChanged}" 
                 Grid.Row="1" 
                 AcceptsReturn="True" 
                 TextWrapping="{Binding WordWrap, Converter={StaticResource BooleanToWordWrapConverter}}" 
                 VerticalScrollBarVisibility="Auto" 
                 HorizontalScrollBarVisibility="Auto"/>
        <StatusBar Grid.Row="2"/>
    </Grid>
</Window>
